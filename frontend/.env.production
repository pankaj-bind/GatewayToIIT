# Vercel injects these at build time (Vite reads only VITE_* vars).
# Keep this file committed so preview deploys pick up the right API URL;
# override per-environment in the Vercel dashboard if needed.
VITE_API_URL=
